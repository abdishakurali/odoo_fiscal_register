<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="product_template_form_view" model="ir.ui.view">
        <field name="name">product.template.form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view" />
        <field name="arch" type="xml">
            <group name="group_general" position="inside">
                <group name="sgr_line" string="SGR">
                    <field name="is_sgr" string="Are SGR"/>
                    <field name="sgr_product_id" string="SGR" invisible="1"/>
                    <field name="sgr_percent" invisible="1"/>
                    <field name="sgr_qty" invisible="1"/>
                    <field name="available_in_pos" string="Disponibil în Franchise Tech?" invisible="1"/>
                    <field name="pos_categ_ids" widget="many2many_tags" groups="point_of_sale.group_pos_user" string="Categorie"/>
                </group>
            </group>
            
            <!-- Hide the Sales tab since when "Can be Sold" is checked, it's automatically available in POS -->
            <page name="sales" position="attributes">
                <attribute name="invisible">1</attribute>
            </page>
            
            <!-- Hide additional fields that exist in the parent view -->
            <field name="description" position="attributes">
                <attribute name="invisible">1</attribute>
            </field>
            
            <!-- Smart buttons are hidden via CSS -->
        </field>
    </record>
</odoo>
